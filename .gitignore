node_modules/
dist/
verification/
vendor/
*.log
data/
.playwright-mcp/
.DS_Store
# Tauri 轨：cargo 构建目录与 staging 生成树（stage.ts 可完整重建）不入库
tauri-app/target/
tauri-app/resources/
# 本地代理工具状态
.zcode/
# 临时调试 / 备份产物不入库
debug-session-dump.cjs
e2e-base.txt
package.json.bak-*
scripts/tmp-dbg*.cjs
scripts/tmp-instrument.cjs
# webui-usage-host 夹具目录（mkdtemp 在 test/ 下创建，正常自清理，中断运行会残留）
test/webui-usage-*/
# build/ 只保留图标与 NSIS 安装脚本，其余构建产物不入库
build/*
!build/icon.ico
!build/icon.png
!build/installer.nsh
# 内置社区插件的 vendor / 自包含运行时依赖 / 离线数据必须随仓库分发，
# 否则克隆后构建出的安装包会复现“插件命名空间不可用”（vendor 缺失）
# 与“tdai-memory 无法启动”（sqlite-vec / jieba / BM25 语料缺失）
!assets/plugins/*/vendor/
!assets/plugins/*/data/
!assets/plugins/dsh-tdai-memory/node_modules/
!assets/plugins/dsh-tdai-memory/node_modules/**/data/

# 工作区临时文件与本地构建输入
temp/

# 工作区一次性临时脚本/探针（约定统一写入 _tmp/，不入库）
_tmp/

# 本地专用指令文件（个人偏好，不进版本库）
AGENTS.local.md
CLAUDE.local.md
